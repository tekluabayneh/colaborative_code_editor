apiVersion: v1
kind: Service
metadata:
  name: backend-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: colab-app-backend
  ports:
    - protocol: TCP
      port: 80 # Port the service is listening on
      targetPort: 5000 # Port the container is listening on (if unset, defaults to equal port value)
