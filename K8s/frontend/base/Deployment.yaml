apiVersion: apps/v1
kind: Deployment
metadata:
  name: colab-frontend
  namespace: colab-app
  labels:
    app: colab-frontend 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: colab-frontend 
  template:
    metadata:
      labels:
        app: colab-frontend 
    spec:
      containers:
        - name: nginx
          image: cgr.dev/chainguard/nginx:latest
          ports:
            - containerPort: 3000
              protocol: TCP
          env:
            - name: PORT
              value: "3000"
          envFrom:
            - secretRef:
                name: colab-backend-secret 
          readinessProbe:
            httpGet:
              path: /
              port: 3000
          resources:
            limits:
              memory: "50Mi"
            requests:
              memory: "50Mi"
              cpu: "250m"
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        runAsUser: 1001
        runAsGroup: 1001
        runAsNonRoot: true

