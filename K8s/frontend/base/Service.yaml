apiVersion: v1
kind: Service
metadata:
  name: frontend-service 
  labels:
    foo: service-label
  annotations:
    bar: service-annotation
spec:
  type: ClusterIP 
  selector:
    baz: pod-label
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 3000 
