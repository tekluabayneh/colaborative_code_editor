apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: colab-app
  labels:
    identifer: service-label
  annotations:
    identifyname: service-annotation
spec:
  type: ClusterIP
  selector:
    app: colab-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
